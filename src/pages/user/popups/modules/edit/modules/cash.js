import React, { useState } from "react";

const CashPopup = props => {
  const [path, setPath] = useState(null);
  const defaultValue = 300;
  const title = `+ ${defaultValue} руб`;

  console.log(props);
  return <></>;
};

export default CashPopup;
